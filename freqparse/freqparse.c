#include <stdio.h>
int main() {
/* count of: 
digits, 
lower-case letters,
other  

*/
  // initialize variables;

 
  char c;
 
  while ((c = getchar()) != EOF) {
    // process one character
    if (c >= '0')
  }
 
  // Output results here
  printf("digits =");
	for (i = 0; i < 10; ++i)
	printf(" %d", ndigit[i]);   
  return 0;
}
